name: release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate:
    uses: ./.github/workflows/package-ci.yaml
    permissions:
      contents: read
      actions: read

  publish:
    needs: [validate]
    if: startsWith(github.ref, 'refs/tags/v') && vars.RELEASE_PUBLISH_ENABLED == 'true'
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    environment: npm-release
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.validate.outputs.source-commit }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '24.15.0'
          registry-url: https://registry.npmjs.org
      - run: npm install --global npm@11.12.1
      - name: Download the successful build's exact artifact ID (also on failed-job reruns)
        uses: actions/download-artifact@v4
        with:
          artifact-ids: ${{ needs.validate.outputs.artifact-id }}
          path: release-artifacts
          merge-multiple: true
      - name: Validate exact source, tag and all local bytes before publication
        run: node npm/release.mjs verify --manifest release-artifacts/manifest.json
      - name: Publish or resume exact immutable npm versions using trusted publishing
        shell: bash
        run: |
          set -o pipefail
          VERSION=$(node -p "require('./release-artifacts/manifest.json').version")
          TAG=latest
          if [[ "$VERSION" == *-* ]]; then TAG=next; fi
          node npm/release.mjs publish --manifest release-artifacts/manifest.json --tag "$TAG" --provenance 2>&1 | tee npm-publication.log
      - name: Preserve publication receipts for exact-artifact resume
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: publication-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            npm-publication.log
            release-artifacts/manifest.json
          if-no-files-found: error
          retention-days: 30

  public-native:
    needs: [validate, publish]
    uses: ./.github/workflows/native-check.yaml
    permissions:
      contents: read
      actions: read
    with:
      artifact-id: ${{ needs.validate.outputs.artifact-id }}
      run-id: ${{ github.run_id }}
      source-commit: ${{ needs.validate.outputs.source-commit }}
      source-ref: ${{ github.ref }}
      public-registry: true

  github-release:
    needs: [validate, public-native]
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.validate.outputs.source-commit }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '24.15.0'
      - run: npm install --global npm@11.12.1
      - name: Download the original validated artifact without rebuilding
        uses: actions/download-artifact@v4
        with:
          artifact-ids: ${{ needs.validate.outputs.artifact-id }}
          path: release-artifacts
          merge-multiple: true
      - name: Publish or resume GitHub assets after all six public consumers succeed
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          set -o pipefail
          node npm/github-release.mjs --manifest release-artifacts/manifest.json 2>&1 | tee github-publication.log
      - name: Preserve GitHub publication receipts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: github-publication-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            github-publication.log
            release-artifacts/manifest.json
          if-no-files-found: error
          retention-days: 30
